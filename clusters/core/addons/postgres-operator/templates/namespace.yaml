# templates/namespace.yaml
{{- if .Values.postgres-operator.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.destinationNamespace | default "postgres-operator" }}
  labels:
    goldilocks.fairwinds.com/enabled: "true"
    pod-security.kubernetes.io/enforce: "privileged"
{{- end }}
