# templates/namespace.yaml
{{- if .Values.opentelemetry-operator.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.destinationNamespace | default "opentelemetry-operator" }}
  labels:
    goldilocks.fairwinds.com/enabled: "true"
    pod-security.kubernetes.io/enforce: "privileged"
{{- end }}# templates/namespace.yaml
{{- if .Values.opentelemetry-operator.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.destinationNamespace | default "opentelemetry-operator" }}
  labels:
    goldilocks.fairwinds.com/enabled: "true"
    pod-security.kubernetes.io/enforce: "privileged"
{{- end }}
